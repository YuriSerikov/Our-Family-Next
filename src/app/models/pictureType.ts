export interface IPicture { 
    filename: string,
    avatar?: string,
    naturalWidth: number,
    naturalHeight: number,
    avatarWidth: number,
    avatarHeight: number,
    photoCaption?: string,
}